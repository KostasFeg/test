import React from "react";

const HelloWorldPage: React.FC = () => (
  <div style={{ padding: "2rem" }}>
    <h1>👋 Hello World</h1>
    <p>This is a demo component rendered via the new hybrid config system.</p>
  </div>
);

export default HelloWorldPage;
